{% for type, message in app.flashes %}
  <div class="my-3">
    <div class="alert alert-{{ type }} alert-dismissible alert-borderless fade show" role="alert">
      <small>{{ message[0]|raw }}</small>
      <button class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
  </div>
{% endfor %}